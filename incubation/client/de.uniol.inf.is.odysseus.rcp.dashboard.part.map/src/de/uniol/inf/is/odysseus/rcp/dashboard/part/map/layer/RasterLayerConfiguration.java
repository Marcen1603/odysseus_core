package de.uniol.inf.is.odysseus.rcp.dashboard.part.map.layer;

public class RasterLayerConfiguration extends LayerConfiguration {
	
	RasterLayerConfiguration(String title, String type, boolean visibility){
		super(visibility, title, type);
	}

}
