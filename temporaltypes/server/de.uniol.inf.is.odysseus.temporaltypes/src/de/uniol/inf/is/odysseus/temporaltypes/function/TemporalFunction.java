package de.uniol.inf.is.odysseus.temporaltypes.function;

/**
 * Maker interface to detect that this function can work directly on temporal
 * types and does not need to get the non-temporal parts (e.g. atMin / atMax).
 * 
 * @author Tobias Brandt
 *
 */
public interface TemporalFunction {

}
