#PARSER PQL
#RUNQUERY


input = ACCESS({source='source', 
  wrapper='GenericPush',
  transport='Camera',
  protocol='None',
  dataHandler='Tuple',
  options=[['deviceid','0']],
  schema=[['Content','Image']]
})

data = MAP({EXPRESSIONS = ['smileDetection(Content)']},input)

