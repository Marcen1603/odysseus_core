#PARSER PQL
#ADDQUERY


source = ACCESS({source='lms1', 
  wrapper='GenericPush',
  transport='Directory',
  protocol='None',
  dataHandler='Tuple',
  options=[['directory','/home/ckuka/tmp/test']],
  schema=[['Content','Image']
  ]
})

out = SELECT({PREDICATE = 'true'}, source)