#PARSER PQL
#ADDQUERY


source = ACCESS({source='source', 
  wrapper='GenericPull',
  transport='File',
  protocol='WAV',
  dataHandler='Tuple',
  options=[['filename','/home/ckuka/tmp/test/example.wav']],
  schema=[['left','Short'],['right','Short']
  ]
})

out = SELECT({PREDICATE = 'true'}, source)