#PARSER PQL
#ADDQUERY

input = ACCESS({source='Source',
wrapper='GenericPush',
transport='Timer',
protocol='None',
dataHandler='Tuple',
options=[['period', '1000']],
schema=[['time', 'Timestamp']]
})

data = MAP({EXPRESSIONS = [['rnd()','x'],['rnd()','y'],['rnd()','z']]}, input)


output = Java({path='${ROOT}/Test.java'}, data)