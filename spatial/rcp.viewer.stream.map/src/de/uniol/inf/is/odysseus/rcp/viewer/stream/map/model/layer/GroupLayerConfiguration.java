package de.uniol.inf.is.odysseus.rcp.viewer.stream.map.model.layer;

public class GroupLayerConfiguration extends LayerConfiguration{

	public GroupLayerConfiguration(String name) {
		setName(name);
	}

}
